const {
  APP_NAME,
  PORT,
  APP_ENV,
  APP_DEBUG,
  APP_URL,
  TIMEZONE,
  LOCALE,
  APP_KEY,
  JWT_SECRET,
  JWT_EXPIRES_IN,
} = process.env;

module.exports = {
  APP_NAME,
  PORT,
  APP_ENV,
  APP_DEBUG,
  APP_URL,
  TIMEZONE,
  LOCALE,
  APP_KEY,
  JWT_SECRET,
  JWT_EXPIRES_IN,
};
